Museum logo centred, origins copy, new ICAIE mark, partners aligned

Museum
- images/mis-eclipse recentred. Measured from the alpha channel: the mark sat
  8px left and 14px high of its canvas, which is why it looked off inside the
  black panel. Now 0,0.
- Origins carries the MIS description: the first-of-its-kind sentence as the
  deck, the exhibitions-and-programming paragraph, and the founding line.

About
- New ICAIE logo installed, trimmed and recentred on a square as before.
- Both partner marks sit in one fixed 96px slot, so the ICAIE logo and the
  RADOC wordmark share a baseline and the names below them align. Verified: both
  headings at the same offset.

Note: three arrangements of the museum page (fixing the black convergence
section bleeding into the black section after it) were produced as previews for
review and are NOT in this commit; the page order is unchanged.

Verified: build 30 pages, 107 asset references present. Alignment measured from
the DOM; partners checked at 1440 and 390.
