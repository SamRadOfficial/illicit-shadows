Load pages at the top, without the scroll animation

- Removed scroll-behavior:smooth from html. It animated every programmatic
  scroll, including the scroll the App Router performs on navigation, so each
  page glided down instead of loading normally. Traced frame by frame:
  2500 -> 2333 -> 256 -> 132 -> 74 over about half a second.
- The html:focus-within variant was tried and removed: clicking a nav link
  focuses it, which re-enabled smooth for that same router scroll.
- Pages now land at 0 instantly, on fresh loads and on in-app navigation. In-page
  anchors jump rather than glide.

Docs
- HANDOFF: why smooth scrolling must not come back, including the focus-within
  trap.

Verified: build 30 pages, 107 asset references present. Three navigations traced
with no intermediate positions, anchor jump instant, fresh load at 0.
