Shop: align the waitlist buttons across each row

- Product cards are flex columns with the control pinned to the bottom, so
  cards stretch to the tallest in their row and the buttons line up whatever the
  description length. Previously a two-line description pushed its button 14px
  below its neighbours.

Verified: build 34 pages, 194 references present. Every row in all five groups
checked at 1440 and 390: no row has buttons at differing offsets.
