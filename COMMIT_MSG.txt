Signup: field and button share one width everywhere

- .signup stacks vertically at every viewport; input and button are both 100%
  of the form's width (420px desktop, 354px at 390). Removed the mobile-only
  wrap rules the stack makes redundant. Button gets a 44px minimum height.

Verified: build 30 pages, 103 asset references present. Widths measured on
home, books and newsroom at 1440 and 390: input and button equal on each.
