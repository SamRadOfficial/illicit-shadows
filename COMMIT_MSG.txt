"The next investigation", and a smaller status chip

- The Continue exploring heading reads "The next investigation."
- The status chip is 9.5px mono, down from body size.

The chip had never been small: it is a <p class="status-chip">, and `.s p`
outranks a bare class, so the 10px rule had been losing silently since it was
written. Same trap as .support-fine and .pview-label. HANDOFF now records it,
with the three classes it has already caught.

Verified: build 34 pages, 194 references present. Chip 9.5px in both places it
appears, contrast 13.9 on ink and 5.1 on cream.
