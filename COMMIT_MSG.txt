Shop: quieter view controls

- The FRONT/BACK stamps are gone. The view is named in 8.5px lowercase mono,
  black at 60% opacity, top left, with no chip behind it.
- One arrow instead of two, top right, same weight and colour, no background.
  With exactly two views a second arrow pointed at the same place.
- Both sit on the mockups' pale grey ground, so black reads better than white
  reversed out.

Fix: the previous rewrite dropped the `flip` helper, so the arrow rendered but
did nothing. Caught by testing the click rather than the markup.

Verified: build 34 pages, 184 references present. Flip works by click, tap and
keyboard, wraps front to back to front, and the aria-label tracks the current
view.
