Intelligence: two columns for "What it is"

- The section used a single measure, leaving the right half of the page empty.
  It is now two columns: the kicker, headline and the modeling question on the
  left, the explanation on the right.
- Pull quote sized for a narrower column (34ch, smaller display size) so it sits
  as a block rather than a six-line ladder.
- Stacks on mobile as before.

Verified: build 30 pages, 107 asset references present. Grid columns confirmed
497.5px each; section checked at 1440 and 390.
