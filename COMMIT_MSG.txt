Add Vercel Analytics, Speed Insights, and Google Analytics 4

- @vercel/analytics and @vercel/speed-insights in the root layout. No
  configuration, no cookies, enabled per project in the Vercel dashboard.
  Verified both scripts load.
- components/Analytics.jsx adds GA4, but only when data/site.json carries a
  measurement ID. The field ships empty, so nothing Google-related loads until
  someone sets it.
- The GA snippet is guarded by hostname, not by NODE_ENV: a static export runs
  identically in a local preview and on the Vercel preview host, and NODE_ENV is
  'production' in a local build too. Verified with a test ID: the loader is
  present, dataLayer is never configured off-domain, and no request reaches
  Google.
- IP anonymisation on, ad personalisation and Google signals off. The readers of
  this site include people researching organized crime; sending less about them
  is the right default.

ROADMAP
- Analytics closed; a new item covers switching them on, which is dashboard work
  rather than code.

Verified: build 34 pages, 194 references present.
