MISTIC copy: subject first, active verbs

- "Its research lab builds Helix.AI. Its fusion center applies that work,
  combining field investigations, museum research, and predictive modeling into
  a single picture of how illicit networks operate."
- Replaces "is where Helix.AI is built" and "is where those capabilities are put
  into practice". The fusion center applies the lab's work, not a finished
  system, which keeps the sentence consistent with Helix.AI being in
  development.
- The full name stays on first mention, since the kicker uses only the acronym.

Verified: build 34 pages, 194 references present. Copy read back from the DOM.
