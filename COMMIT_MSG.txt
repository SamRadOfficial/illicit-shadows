Intelligence: hero line removed, buyer cards evened out

- Removed "Causal AI on a global risk graph" from the hero; the sentence above it
  already says causal AI. The phrase stays in the flywheel.
- Buyer cards: one-word headings (Corporate, Government, International), who-
  lines of 48 to 51 characters, and descriptions of 114 to 118, so every row runs
  the same number of lines.
- The cards share grid rows (subgrid), so the heading, who-line and description
  line up across all three even when a width changes the wrapping. Stacked on
  phones.
- "Law enforcement" moves into the Government who-line; the International
  description now ends "so programs, due diligence and policy land where they
  matter."

Verified: rows aligned at 1440, 1100 and 900 with equal line counts in each; no
overflow at 390.
