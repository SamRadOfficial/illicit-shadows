Rename the production post URL; MIS banner on the museum post

- Slug follows the title: /newsroom/production-begins-chemical-cartels. The old
  /newsroom/production-begins-fentanyl-cartels now 301s to it in next.config.js,
  so anything already shared keeps working. Note that redirects run on Vercel,
  not in the static export, so the local preview will 404 the old path.
- The museum announcement leads with the MIS presentation banner rather than the
  square mark, at the wide frame.

Verified: build 32 pages, 150 references present. Both posts return 200 at their
new paths, index links and sitemap updated.
