Left-align the giving tiers

- .tiers was centred by a rule left over from the old donor block. The row now
  starts at the same left edge as the copy above it, and each tier's own text is
  explicitly left-aligned.
- Verified the first tier's left edge matches the paragraph's on /donate, home,
  film, about and newsroom.

Noticed while checking: /museum no longer has its own donor section. It was
removed when the shared band landed and the page now relies on that, which is
the intended one-ask-per-page behaviour, but worth confirming you are happy the
museum has no donate block of its own above the footer.

Verified: build 32 pages, 150 references present.
