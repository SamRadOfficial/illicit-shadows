Restore the MIS eclipse, and raise the type scale one step

- images/mis-eclipse is the supplied original again. My alpha-cut version had
  mangled the corona into a spiky feathered edge. The mark only appears inside
  .black-logo on /museum, which is already a black panel, so the artwork's own
  black field is correct and no transparency is needed. .black-logo now frames
  the image edge to edge.
- Type scale A: body 15 to 16.5px, small print 11 to 12px, labels 11.5px, nav
  14.5 to 15.5px, deck 21px, hero lede 17px, and the card and list sizes lifted
  a step to match. Headings unchanged, so the contrast between headline and text
  is preserved.

Verified: build 30 pages, 108 asset references present. Computed sizes read back
from the DOM; all fourteen routes checked at 1440, 390 and 320 with no
horizontal overflow.
