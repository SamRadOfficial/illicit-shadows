About: keep MEDIA · KNOWLEDGE · INTELLIGENCE on one line

- .text-routes no longer wraps. The three words are one statement, so the size
  now follows the viewport, clamp(15px, 3.1vw, 24px), rather than the row
  breaking when it runs out of room. Arrows and gaps scale with the type (em
  units) instead of staying fixed.

Verified: one row at 320, 360, 390, 430, 768, 1024 and 1440, with no overflow
inside the row or on the page.
