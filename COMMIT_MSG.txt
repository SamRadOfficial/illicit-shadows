Home description matches the hero

- "Media, knowledge, and intelligence. We investigate the $6 trillion shadow
  economy, expose the systems behind it, and model how its networks adapt." 146
  characters, inside what Google displays, and identical to the line a visitor
  reads at the top of the page.
- Title unchanged: "Illicit Shadows: Investigating the Global Shadow Economy",
  which carries the words people actually search.
- The site-wide fallback in data/site.json now matches, so any page that does
  not set its own description says the same thing.

Verified: build 34 pages, 194 references present. Audit clean: no duplicate
titles or descriptions, and the description reaches og and twitter too.
