Partners section on white, with a transparent ICAIE mark

- #partner-orgs moves to the light surface, which also breaks up the run of dark
  sections before the yellow CTA.
- ICAIE logo cut out of its white field: flood-filled from the corners, with
  partial alpha derived from each edge pixel's distance from white and the
  colour unpremultiplied, so the outline carries no white fringe on cream.
  Shipped as PNG plus WebP; the JPEG is gone, since it cannot hold alpha.
- Both marks centred at 132px, names and links centred beneath. RADOC keeps its
  black OC highlight.
- Fixed rule order: .partners .plogo img came after .plogo-big img, so the
  smaller size and the old white plate were winning.

Intelligence (option B) and the contact map query are in this commit as well.

Verified: build 30 pages, 106 asset references present. Logo renders 132x132
with no background, names level, checked at 1440 and 390; cut-out proofed on
cream and on black before shipping.
