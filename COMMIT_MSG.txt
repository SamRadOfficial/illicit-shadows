Divide same-surface sections; trim the Intelligence lede

- Sections that sit next to another section on the same surface now carry a 1px
  top rule at content width. A change of surface is already a divider, so only
  same-colour neighbours get the line. Audited every route: six pages had such a
  run, on /museum, /intelligence, /books, both investigation pages and the short
  page. All now separated; nothing else changed.
- Intelligence hero drops "Not a forecast of events, but of adaptation."

Verified: build 30 pages, 106 asset references present. Every same-surface
boundary confirmed at 1px; boundaries inspected at 1440.
