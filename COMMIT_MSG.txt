Film: the Illicit Gold cover leads the section on mobile

- Below 820px the gold feature stacks image first, matching the museum and books
  features, which were already image-first. /film was the outlier.
- Done with flex column-reverse rather than a markup change: the section has
  exactly two children, so no wrapper, no new grid rule, and desktop is
  untouched.

Verified: build 34 pages, 194 references present. Order read from the DOM at
1440 (eyebrow, headline, image in its column), and image-first at 820, 390 and
320.
