Fix black-on-black surfaces, and the repeated title on the shorts section

- Surface layer no longer uses z-index:-1. A negative layer disappears behind
  any ancestor that creates a stacking context, which is why the cream reading
  section on /film/chemical-cartels rendered as black text on a black page once
  deployed while looking correct in local preview. Sections are now
  isolation:isolate, the ::before sits at z-index 0, and content is lifted to 1.
- Removed legacy .credits, .direct and .reading rules left from the
  pre-editorial layout. They carried a dark panel background and painted over
  the cream surfaces; the credits block on the investigation pages was the
  visible case.
- The short-films section repeated the film title as its kicker directly under
  the page title. It now reads the form ("Eleven short films").

Verified: build 30 pages, 100 asset references present. Contrast audited across
fourteen routes, every paragraph, heading, list and definition item against its
resolved background: zero below 4.5:1. Lightbox still covers the full viewport
after the layering change.
