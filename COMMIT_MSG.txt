Museum: one action in the prototype section

- Removed the exhibition-updates signup. The section now offers only "Enter the
  museum concept", which is the thing to do there; two stacked yellow controls
  read as one broken button anyway.
- Dropped the Signup and site imports, which the page no longer uses.

Note: the museum page now has no exhibition-openings signup at all. The Umbra
Circle and Helix.AI notify blocks are unaffected. If you want exhibition updates
captured somewhere, the patron section or the halls block are better homes than
a prototype CTA.

Verified: build 34 pages, 194 references present. One action in the section, no
signup anywhere on the page.
