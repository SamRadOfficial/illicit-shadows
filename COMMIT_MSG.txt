Contact trims, wider map, and a real museum shop section

Contact
- Note under Send message reads only "Expect a reply within a few working days."
- "What is this about?" legend removed; the fieldset keeps an aria-label so the
  checkboxes still announce what they are for.
- Map zoomed out one step (z=14) so the White House sits in frame.
- "Directions" link removed; Open in Google Maps remains.

Museum
- The gift shop is its own section rather than a one-line band: "Take the museum
  home with you", three numbered categories (exhibition editions, publications,
  founding-donor editions), and a Join the shop list CTA. It states plainly that
  it opens with Phase I in 2027 and that purchases fund the exhibitions.
- Built it yellow first, then moved it to the ink surface: the donor band
  directly above is yellow, and two yellow bands in a row break the rule that
  the signal colour appears once per page. It now contrasts with the donor
  moment instead of competing with it.

Fix
- .ed-btn was only surface-aware for light surfaces, so on ink and slate it
  inherited the wrong colour and could render yellow-on-yellow (1.1:1). Buttons
  are now explicit per surface; audited every .ed-btn on six routes at two
  widths, nothing below 4.5:1.

Verified: build 30 pages, 148 references present. Shop section contrast clean,
no horizontal overflow at 390.
